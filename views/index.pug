doctype html
html
  head
    meta(charset="utf-8")
    title OCR Demo
  body
    h1 OCR Demo

    p Upload a file and we’ll find stuff in it
    form(method="post", action="/recognize")
      input(type="hidden", name="secret", value="123")
      input(type="text", name="comment")
      input(type="submit")

    p Yeah next round we’ll do file upload
